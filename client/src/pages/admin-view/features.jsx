// Define o componente AdminFeatures que será usado para exibir os recursos de administração.
function AdminFeatures() {
  // Retorna um elemento div com o texto "Recursos da Administração".
  return <div>Recursos da Administração</div>;
}

// Exporta o componente AdminFeatures como padrão para que possa ser utilizado em outras partes da aplicação.
export default AdminFeatures;
